apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lora-training-pvc
  namespace: openclaw
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-ssd
  resources:
    requests:
      storage: 150Gi
