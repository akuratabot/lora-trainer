apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lora-training-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 130Gi
